# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- dev

pool:
 name: KDIOTPool
 demands: docker

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'Koç Holding Ayrımcı Dil Container Registry'
    repository: 'ayrimcidilapi'
    command: 'buildAndPush'
    Dockerfile: '**/api/Dockerfile'
    tags: 'build-tag-1'